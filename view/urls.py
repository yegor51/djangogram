from django.urls import path
from . import views

urlpatterns = [
    #path('profile/<int:id>/', views.profile, name='view_profile'),
    #path('publication/<int:id>/', views.publication, name='view_publication'),
    #path('all-users/', views.all_users, name='view_all_users')
]